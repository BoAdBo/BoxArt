class Solution {
 public:
  double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
    std::size_t mid1 = nums1.size() / 2, mid2 = nums2.size() / 2;
    std::size_t len = std::min(nums1.size(), nums2.size());

    // hard to implement
  }
};
