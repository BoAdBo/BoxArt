#include "predefined.cpp"

class Solution {
public:
  bool isMatch(string s, string p) {
    // match the substring that is not asterisk first, then match asterisk
    for( int i = 0; i < p.size(); ++ i ) {
      if( p[i] == '*') {
        
      }

    }
  }
};
